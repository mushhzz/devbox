[defaults]
inventory = inventory/localhost.yml
roles_path = roles
callbacks_enabled = ansible.builtin.default
result_format = yaml
gathering = smart

[privilege_escalation]
become = false
become_method = sudo

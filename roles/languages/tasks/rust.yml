---
- name: Install rustup and Rust toolchain
  ansible.builtin.shell: >
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs |
    sh -s -- -y --default-toolchain {{ rust_toolchain }}
  args:
    creates: "{{ target_user_home }}/.rustup"
